/**
 * Route guards — re-exported from centralized routing module.
 * Prefer importing from `src/routes/guards` in new code.
 */
export { AuthGate, GuestRoute, ProtectedRoute } from '../routes/guards'
export type { AuthRedirectState } from '../routes/guards'
